# ---------- API base URL ----------
# Where the backend lives, WITHOUT the /api/v1 suffix (the app appends it).
# In development this defaults to http://localhost:8000, so this file is only
# required when the backend is hosted elsewhere (production / preview deploys).
VITE_API_URL=http://localhost:8000
